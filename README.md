# Kinesis sample code for the arc workshop

## pre requisites

* Java (build and tested with version 13)
* aws (`~/.aws/`) setup and `[profile arc]` configured and pointing to a valid assume role

## run

    ./gradlew run